<%= form_with(model: school_category) do |form| %>
  <% if school_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(school_category.errors.count, "error") %> prohibited this school_category from being saved:</h2>

      <ul>
        <% school_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
