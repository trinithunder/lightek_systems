<%= form_with(model: admin_dashboard) do |form| %>
  <% if admin_dashboard.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_dashboard.errors.count, "error") %> prohibited this admin_dashboard from being saved:</h2>

      <ul>
        <% admin_dashboard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
