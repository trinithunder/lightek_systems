<%= form_with(model: follow) do |form| %>
  <% if follow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(follow.errors.count, "error") %> prohibited this follow from being saved:</h2>

      <ul>
        <% follow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :follow, style: "display: block" %>
    <%= form.number_field :follow %>
  </div>

  <div>
    <%= form.label :following, style: "display: block" %>
    <%= form.number_field :following %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
