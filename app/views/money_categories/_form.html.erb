<%= form_with(model: money_category) do |form| %>
  <% if money_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(money_category.errors.count, "error") %> prohibited this money_category from being saved:</h2>

      <ul>
        <% money_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
